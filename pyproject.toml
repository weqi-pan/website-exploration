[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "website-exploration"
version = "1.0.0"
requires-python = ">=3.10"
dependencies = ["playwright==1.61.0", "python-docx>=1.0"]
